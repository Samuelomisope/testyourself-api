CREATE TABLE IF NOT EXISTS "Feedback" (id TEXT PRIMARY KEY, "userId" TEXT REFERENCES "User"(id), rating INTEGER NOT NULL, category TEXT NOT NULL, message TEXT NOT NULL, page TEXT, "createdAt" TIMESTAMP NOT NULL DEFAULT NOW());
